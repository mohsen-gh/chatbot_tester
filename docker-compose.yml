version: '3.8'

services:
  chatbot:
    build:
      context: .
      dockerfile: chatbot/Dockerfile
    container_name: chatbot-app
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
